# Phase 3 – Local Installation (Self-Owned Environment)

## Objective

The objective of this phase is to install the OpenROAD Flow Scripts (ORFS) and OpenROAD on my local Ubuntu environment. This phase helps in understanding the installation process and preparing a self-owned environment for executing the RTL-to-GDS flow.

---

# ORFS Installation

The following steps were performed:

1. Cloned the ORFS repository.
2. Installed the required dependencies.
3. Verified that the `make` utility was available.
4. Configured the required environment.

---

# OpenROAD Installation

The OpenROAD installation included:

- Downloading and installing OpenROAD.
- Verifying the installation.
- Configuring the required libraries and environment variables.

---

# Verification

The following commands were executed successfully:

```bash
openroad -version
```

```bash
yosys -V
```

```bash
which openroad
```

Screenshots of these commands are available in the **Screenshots** folder.

---
